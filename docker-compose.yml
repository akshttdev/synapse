services:
  backend:
    build: ./backend
    image: synapse-backend
    ports:
      - "8000:8000"
    env_file:
      - ./backend/.env
    environment:
      QDRANT_API_KEY: ${QDRANT_API_KEY}
    volumes:
      - ./backend:/app

volumes:
  data:
